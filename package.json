{
  "name": "realcash-backend",
  "version": "1.0.0",
  "private": true,
  "description": "RealCash (ريل كاش) — Telegram Mini App backend on Vercel Serverless + Neon Postgres",
  "dependencies": {
    "@neondatabase/serverless": "^0.9.0"
  }
}
